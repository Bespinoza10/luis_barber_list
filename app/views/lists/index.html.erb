<div class="container">
  <div class="row">
    <div class="col-sm-2 list center-block">
      <%= link_to "Add Me to the List", new_list_path, class: " center-block btn-add btn"%>
    </div>
    <br>
    <div id="lists" class=" transitions-enabled list center-block">

      <% @barber.each do |barber| %>

        <div class="boxy names panel-default panel">
          <h1><%= barber.name %></h1>
          <% for list in barber.lists %>
              <h4><%= link_to list.name, list %></h4>
          <% end %>
          <hr>
        </div>

      <%end%>

    </div>
  </div>
</div>
